name: Test

on:
  push:
    branches:
      - develop
      - main
      - 'support/*'
  pull_request:
  workflow_dispatch:

jobs:
  test:
    uses: ardge-labs/github-workflows/.github/workflows/go-test.yml@v1
    with:
      go-version: '1.25.4'
    secrets:
      ARDGE_GITHUB_PAT: ${{ secrets.ARDGE_GITHUB_PAT }}
